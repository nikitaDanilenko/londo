name: Auto-merge Scala Steward Pull Requests

on:
  pull_request:
    branches:
      - main

jobs:
  auto_merge:
    name: Auto-merge Scala Steward PRs
    permissions:
      contents: write
      pull-requests: write
    if: github.event.pull_request.user.login == 'scala-steward-bot[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Auto-merge
        uses: ./.github/actions/auto-merge
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
